package content;
/*
import mindustry.content.Fx;
import mindustry.entities.bullet.BasicBulletType;
import mindustry.type.Category;
import mindustry.type.ItemStack;
import mindustry.world.Block;
import mindustry.world.blocks.defense.turrets.ItemTurret;

public class NewPT {
    public static Block NGM;
    public static void NewP(){
        float range = 500f;
        NGM=new ItemTurret("capt"){{
        requirements(Category.turret, ItemStack.with(Items.oreZeta,91));
        ammo(
              Items.oreZeta, new BasicBulletType(2.9f,9){{
                    shootEffect = Fx.instShoot;
                    hitEffect = Fx.instHit;
                 //   pierceEffect = Fx.railHit;
                    smokeEffect = Fx.smokeCloud;
                 //   pointEffect = Fx.instTrail;
                    despawnEffect = Fx.instBomb;
                 //   pointEffectSpace = 20f;
                    damage = 1350;
                    buildingDamageMultiplier = 9.0f;
                    maxDamageFraction = 0.6f;
                    pierceDamageFactor = 1f;
                    //length = brange;
                    hitShake = 6f;
                    ammoMultiplier = 1f;
                }}
        );
            maxAmmo = 400;        // 最大弹药容量
            ammoPerShot = 2;     // 每次射击消耗5单位弹药
            reload = 20f;             // 长装填时间(200帧≈3.3秒)
            rotateSpeed = 2f;          // 慢速旋转
            shootCone = 2f;            // 精确射击角度
           // shootSound = Sounds.railgun;  轨道枪音效
            recoil = 5f;               // 明显后坐力
            shake = 4f;                // 屏幕震动效果
            cooldownTime = reload;     // 冷却时间=装填时间
            ammoUseEffect = Fx.casing3Double; // 弹药消耗特效
        }};
    }
}
*/